{
  "name": "deutsch-a2-learning",
  "private": true,
  "scripts": {
    "build:dictionary": "node scripts/build_dictionary_mobile_assets.js",
    "test": "npm run build:dictionary && node --test"
  }
}
